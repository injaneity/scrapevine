Act as a high-level JSON data analysis provider, meant to isolate key information from complicated JSON files on a single product.
Since the provided JSON object you are receiving is a processed version of a website's HTML, there are some values that may not be necessary.
From the file I give you, analyse the data based on its proximity to each other and relevance, and return me:
    
    1. Name of the product (unique)
    2. Price of the product (unique for product with only one variant, and can be a range for products with multiple variants)
    3. Description of the product (unique)

Return this data in the form of a JSON object. Price can be a list if there are multiple values you feel are relevant. The JSON object is as follows: